eureka:
  client:
    serviceUrl:
      defaultZone: http://dockereurekaserver:8761/eureka/ # 这个需要改为镜像名
  instance:
      preferIpAddress: true
server:
  port: 8763
spring:
  application:
    name: register-service-hi